アプリ「Microsoft To Do(以下TODO)」でタスクの管理をしている。
課題としてタスクの"優先順位付けに時間がかかること"がある。
原因は、私が優先順位付けに使いたい「緊急度、重要度、解像度」の情報を入力することが出来ないからである。
そこで、これを解決するアプリケーションを開発したい。

## アプリケーション名
todoresolution

## アプリケーション開発への要求
0. 緊急度、重要度、解像度は、0～10の実数とする。
1. todoresolutionはGUIアプリケーションで、
　　3つのボタンとバブルチャート、テーブルを持つ。
2. todoresolutonのGUIは左側にバブルチャートを6.5割の大きさで表示し、
　　残りの部分にテーブルを表示する。
3. ボタンは「Extract」「Reset」「Close」の順に並べる。
　　「Reset」を押下すると、全タスクの(重要度、緊急度)を(5, 5)にリセットする(解像度は変更しない)。
4. 「Extract」を押下すると
　　Outlook の「受信トレイ」にあるメールのうち件名が「Microsoft To Do - 今日の予定」のメールをよみこむ。

【以下、内容の例】
---
今日の予定

⬜ タスクA - 期限 今日
⬜ タスクB - 期限 今日
        ⬜ サブタスクB-1
        ⬜ サブタスクB-2
⬜ タスクC - 期限 今日
        ⬜ サブタスクC-1
        ✅ サブタスクC-2(完了済み)
        Notes: メッセージへのリンク<https://...>
⬜ タスクD - 期限 今日
---

5. この内容について、TOPレベルの表示のもののみタスクとしてtodoresolution内に取り込む。
　　(インデントされたサブタスクやNotes等は取り込まない)
このとき、既に読み込んだタスク(旧タスク群)があれば、新しく読み込んだタスク(新タスク群)とくらべ、
新タスク群にのみあるタスクは追加し、旧タスク群のみにあるタスクは削除し、
両方に存在するタスクについては、一度定義した「重要度、緊急度、解像度」を含めそのままにする。

【上記内容例でタスクとして読み込むもの】
⬜ タスクA - 期限 今日
⬜ タスクB - 期限 今日
⬜ タスクC - 期限 今日
⬜ タスクD - 期限 今日

6. todoresolution上のバブルチャートのGUIで
　　Drag&Dropにて、これらタスクの緊急度、重要度、解像度を定義できるようにしたい。
　　横軸が重要度、縦軸が緊急度として、バブルの大きさは(10-解像度)とする。
　　各軸の数値は逆進とする。つまり、原点が(10, 10)で各軸終端が(10, 0),(0, 10)。
7. 定義された緊急度、重要度、解像度から以下の式で総合評価を計算して、GUI
    総合評価=重要度²×(緊急度 + 0.5×(10-解像度))
8. 右のテーブルは、「総合評価」「重要度」「緊急度」「解像度」「タスク名」のカラムをもたせる。
Drag&Dropで定義される重要度、緊急度、解像度、および計算される総合評価の情報をリアルタイムにテーブルに反映し、常に「総合評価」で降順にソートして表示する。
9. データベース(sqlite)を持ち、一度定義した「重要度、緊急度、解像度」は再利用できるようにする。
　　「Close」で閉じるときには、タスク情報(タスク名、重要度、緊急度、解像度)をデータベースに保存し、
　　todoresolutionを再起動したときは、データベースからタスクを読み込んで、最後の状態を再現する。

【件名】「Microsoft To Do - 今日の予定」のメールは、実際にOutlookの受信トレイに置いてある。

## 実装方針
- To Do への書き戻し(Sync)は行わない。バブルチャート/テーブルで優先順位を確認するツールとする。
- Microsoft Graph API(OAuth/Azure ADアプリ登録)は使用しない。Outlook COM(pywin32)でローカルのOutlookデスクトップアプリから受信トレイのメール本文を直接読み込む。
- タスクの同一性はタスク名(文字列)で判定する(メール側に安定した一意IDが無いため)。同名タスクが複数存在する場合は区別できない点に留意。
- 実装言語: Python (Tkinter + Matplotlib + sqlite3)。
